{
  "name": "mcp-review-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=agent",
    "dev:agent": "npm run dev --workspace=agent",
    "dev:server": "npm run dev --workspace=mcp-server"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.7.0"
  }
}
